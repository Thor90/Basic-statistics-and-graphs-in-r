2. Data manipulation

head(dataset)
names(dataset) 
str(dataset) # get structure
==  # equal to
!=  # not equal to
>   # greater than
<   # less than
>=  # greater than or equal to
<=  # less than or equal to
&   # and
|   # or
